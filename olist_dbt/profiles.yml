# Connection settings for this project.
# Kept inside the repo (not in ~/.dbt) so the project is reproducible by anyone, including CI.
olist_dbt:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: ../olist.duckdb      # the database created by scripts/01_load_raw.py
      threads: 4
